import google.generativeai as genai
import os
import logging
import httpx
from dotenv import load_dotenv

load_dotenv()
logger = logging.getLogger(__name__)

GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")
GROQ_API_KEY = os.getenv("GROQ_API_KEY")

if GEMINI_API_KEY:
    genai.configure(api_key=GEMINI_API_KEY)
elif not GROQ_API_KEY:
    logger.warning("Neither GEMINI_API_KEY nor GROQ_API_KEY is set. AI features will be disabled.")

async def generate_cover_letter(vacancy_description: str, user_cv: str, is_telegram: bool = False) -> str:
    if not GEMINI_API_KEY and not GROQ_API_KEY:
        return "Neither Gemini nor Groq API key is configured. Please set GEMINI_API_KEY or GROQ_API_KEY."
        
    if is_telegram:
        prompt = f"""
Ты — профессиональный C#/.NET и React разработчик с высоким уровнем когнитивной эмпатии и навыков B2B-аутрича.
Твоя задача — написать емкое, хирургически точное приветственное сообщение (elevator pitch) рекрутеру в Telegram.
Оно должно подчиняться законам холодного Outbound-аутрича и фреймворку AIDA/AIDCA.

КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО:
- Использовать скучные ИИ-штампы ("Откликаюсь на позицию...", "Мой стек подходит...", "Привет! Увидел вакансию...").
- Начинать с пустого приветствия без сути ("Добрый день!" с ожиданием ответа).
- Использовать любые плейсхолдеры в квадратных скобках или угловых скобках (заглушки вида `[ссылка]`, `[вставить]`, `<компания>`, `[резюме]`, `[Имя]`, `{{компания}}`).
- Описывать микроменеджмент, жаловаться на жизнь или выражать социальное отчаяние.
- Писать сплошным текстом без абзацев.

ЕСЛИ В ДАННЫХ КАНДИДАТА ИЛИ ВАКАНСИИ НЕТ КАКОГО-ТО ЗНАЧЕНИЯ (например, имени, конкретных цифр или ссылки), НЕ ПРИДУМЫВАЙ ВЫМЫШЛЕННЫЕ ССЫЛКИ И НЕ ОСТАВЛЯЙ ШАБЛОННЫЕ ТЕГИ. Вместо этого сформулируй предложение естественным языком без упоминания отсутствующей детали (например: "Мой код и проекты можно посмотреть на GitHub (ссылку прикрепил к отклику)").

КАК СТРОИТЬ СООБЩЕНИЕ ДЛЯ TELEGRAM (Фреймворк AIDA/AIDCA):
1. Attention (Внимание & Раппорт): 
   - Начни с приветствия по имени (если оно есть в вакансии, например: "Здравствуйте, Анна!"). Если имени нет, просто "Здравствуйте!" или "Приветствую!".
   - Сразу дай хук: упомяни интересный профессиональный факт о компании, стеке или их продукте. Пример: "Вижу, что ваша команда активно масштабирует финтех-платформу...", "Интересное решение с переходом на микросервисы...".
2. Interest (Интерес к болям компании): 
   - Не рассказывай долго о себе. Сфокусируйся на проблемах и задачах компании. 
   - Напрямую свяжи их стек/задачи с твоим опытом. Пример: "Предполагаю, что при росте нагрузки на API сейчас важна оптимизация запросов к Postgres. Недавно я как раз занимался аналогичной задачей..."
3. Desire & Confidence (Желание & Доверие):
   - Опиши свое ценностное предложение (Value Proposition) через оцифрованные достижения и активные глаголы ("сократил время отклика API на 40%", "покрыл тестами 85% кода", "разработал UI-кит на React").
   - Дай верифицируемые ссылки (GitHub, резюме) без квадратных скобок. Если в CV пользователя есть ссылки, вставь их естественно: "Мой код на GitHub: github.com/...". Если ссылок нет в CV, напиши: "Резюме и код на GitHub прикрепил ниже".
4. Action (Призыв к действию):
   - Задай простой, открытый вопрос, требующий минимума усилий для ответа. Пример: "Подскажите, с кем в команде можно коротко обсудить детали, чтобы понять, насколько мой стек будет вам сейчас полезен?"

ТРЕБОВАНИЯ К ФОРМАТУ И СТИЛЮ:
- Объем: максимально сжато, до 100-120 слов (помещается на 1 экран смартфона).
- Микро-абзацы: не более двух предложений в каждом.
- Простой синтаксис: никаких громоздких деепричастных оборотов и канцелярщины.
- Адаптация под уровень кандидата (определи его уровень по CV):
  * Junior: акцент на академические проекты, пед-проекты, скорость обучения, готовность брать рутину.
  * Switcher: переносимые навыки (трансферные навыки из прошлых сфер), осознанный переход, свежие курсы.
  * Senior: масштаб ответственности, архитектурные решения, бизнес-метрики (время/деньги).

ДАННЫЕ О КАНДИДАТЕ (Используй для фактов):
{user_cv}

ОПИСАНИЕ ВАКАНСИИ (Выдели боли и ключевые слова):
{vacancy_description}
"""
    else:
        prompt = f"""
Ты — опытный C#/.NET и React разработчик с отличным пониманием когнитивной психологии рекрутмента.
Твоя задача — написать структурированное, строгое, информативное сопроводительное письмо для HH.ru (Inbound-отклик).
Оно должно выступать смысловым интерпретатором опыта кандидата, а не просто копировать его резюме.

КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО:
- Использовать фразы: "Я пишу по поводу вашей вакансии...", "Предлагаю ознакомиться с моим резюме...", "Внимательно ознакомился с вашей вакансией...".
- Использовать любые плейсхолдеры и скобки вроде `[ссылка]`, `<компания>`. Если ссылки нет — просто не упоминай её, кандидат прикрепит резюме стандартной кнопкой HH.
- Использовать пустые слова-штампы (клише): "коммуникабельный", "ответственный", "дисциплинированный", "быстрообучаемый", "целеустремленный", "стрессоустойчивый". Переводи их в факты (например, стрессоустойчивость -> "сдал сложный проект в условиях горящего дедлайна").
- Обсуждать условия работы, график, зарплату или удаленку на этом этапе.
- Проявлять токсичность, жаловаться на прошлых работодателей или демонстрировать социальное отчаяние ("срочно нужна работа").
- Писать сплошным текстом (простыней).

ЕСЛИ В ДАННЫХ КАНДИДАТА ИЛИ ВАКАНСИИ НЕТ КАКОГО-ТО ЗНАЧЕНИЯ (например, имени, конкретных цифр или ссылки), НЕ ПРИДУМЫВАЙ ВЫМЫШЛЕННЫЕ ССЫЛКИ И НЕ ОСТАВЛЯЙ ШАБЛОННЫЕ ТЕГИ. Вместо этого сформулируй предложение естественным языком без упоминания отсутствующей детали.

АРХИТЕКТУРА СОПРОВОДИТЕЛЬНОГО ПИСЬМА HH.RU:
1. Приветствие и Вступление (не более 2-3 предложений):
   - Уважительное приветствие. Если известно имя рекрутера из вакансии, обратись лично ("Здравствуйте, Анна"). Если нет, то "Здравствуйте!" или "Добрый день, команда [Название компании/бренда]" (если название компании известно).
   - Четко укажи позицию и профессиональную мотивацию. Мотивация должна быть связана с фактами о компании (их стек, ценности, сфера).
2. Семантическое ядро (1-2 абзаца, по 3-5 строк):
   - Сопоставь ключевые требования вакансии с реальным опытом кандидата.
   - Описывай достижения с помощью сильных активных глаголов ("разработал", "построил", "внедрил", "выполнил") в совершенном виде. Пенализируй отглагольные существительные и страдательный залог ("занимался ведением").
   - ОБЯЗАТЕЛЬНО используй оцифрованные метрики и результаты (например: "увеличил скорость загрузки UI на 30%", "сократил время сборки проекта", "интегрировал 5 внешних API"). Выбери 2 ключевых проекта соискателя, которые закрывают боли работодателя.
3. Заключение (1-2 емких предложения):
   - Транслируй конструктивный, деловой настрой и готовность к предметному техническому диалогу.
4. Контакты и подпись:
   - Корректная нейтральная подпись с указанием телефона и Telegram (если они есть в CV, без плейсхолдеров).

ТРЕБОВАНИЯ К ФОРМАТУ И СТИЛЮ:
- Объем: до 150-180 слов (примерно половина листа A4).
- Строгая разметка: разделяй смысловые абзацы двойным переносом строки (пустая строка между абзацами). Внутри абзаца — одинарный интервал.
- Адаптация под уровень кандидата (определи его уровень по CV):
  * Junior: акцент на академический бэкграунд, учебные проекты, хакатоны, диплом.
  * Switcher: переносимые навыки (аналитика, менеджмент), осознанность выбора новой профессии.
  * Senior: фокус на архитектуру, управление техническим долгом, влияние решений на бизнес.

ДАННЫЕ О КАНДИДАТЕ (Используй для фактов):
{user_cv}

ОПИСАНИЕ ВАКАНСИИ (Выдели требования и ключевые слова):
{vacancy_description}
"""

    if GROQ_API_KEY:
        try:
            url = "https://api.groq.com/openai/v1/chat/completions"
            headers = {
                "Authorization": f"Bearer {GROQ_API_KEY}",
                "Content-Type": "application/json"
            }
            payload = {
                "model": "llama-3.3-70b-versatile",
                "messages": [
                    {"role": "user", "content": prompt}
                ],
                "temperature": 0.7
            }
            async with httpx.AsyncClient() as client:
                response = await client.post(url, json=payload, headers=headers)
                response.raise_for_status()
                return response.json()["choices"][0]["message"]["content"].strip()
        except Exception as e:
            logger.error(f"Error generating cover letter via Groq: {e}")
            return f"Ошибка генерации сопроводительного письма (Groq): {str(e)}"
    else:
        try:
            model = genai.GenerativeModel('gemini-3.5-flash')
            response = await model.generate_content_async(prompt)
            return response.text.strip()
        except Exception as e:
            logger.error(f"Error generating cover letter: {e}")
            return f"Ошибка генерации сопроводительного письма: {str(e)}"

async def analyze_vacancy(vacancy_description: str) -> str:
    if not GEMINI_API_KEY and not GROQ_API_KEY:
        return ""
        
    prompt = f"""
Проанализируй описание вакансии и выдели главное в формате Markdown:
1. Ключевые требования (буллет-поинты).
2. Основной стек технологий (список через запятую).
3. Плюсы и минусы вакансии (кратко).

ВАКАНСИЯ:
{vacancy_description}
"""

    if GROQ_API_KEY:
        try:
            url = "https://api.groq.com/openai/v1/chat/completions"
            headers = {
                "Authorization": f"Bearer {GROQ_API_KEY}",
                "Content-Type": "application/json"
            }
            payload = {
                "model": "llama-3.3-70b-versatile",
                "messages": [
                    {"role": "user", "content": prompt}
                ],
                "temperature": 0.5
            }
            async with httpx.AsyncClient() as client:
                response = await client.post(url, json=payload, headers=headers)
                response.raise_for_status()
                return response.json()["choices"][0]["message"]["content"].strip()
        except Exception as e:
            logger.error(f"Error analyzing vacancy via Groq: {e}")
            return ""
    else:
        try:
            model = genai.GenerativeModel('gemini-3.5-flash')
            response = await model.generate_content_async(prompt)
            return response.text.strip()
        except Exception as e:
            logger.error(f"Error analyzing vacancy: {e}")
            return ""
